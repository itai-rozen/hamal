/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    './app/**/*.tsx', // Note the addition of the `app` directory.
    './app/*.tsx', // Note the addition of the `app` directory.
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}

